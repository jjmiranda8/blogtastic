<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

  <%= link_to "BLOGTASTICAL!", root_path, class: "navbar-brand", id: "logo" %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
           Profile  
        </a>
                
      <div class="dropdown-menu bg-dark"  aria-labelledby="navbarDropdownMenuLink">
        <% if logged_in? %>
          <%= link_to "My profile", user_path(current_user), class: "nav-link" %>
          <%= link_to "Logout", logout_path, class: "nav-link", method: :delete %>
          <%= link_to "Delete profile", user_path(current_user), class: "ndropdown-item nav-link text-danger", method: :delete, data: { confirm: "Are you sure" } %>
        <% else %>
          <%= link_to "Login", login_path, class: "nav-link" %>
          <%= link_to "Sign up", signup_path, class: "nav-link" %>
        <% end %>
      </div>

      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Articles
        </a>
                
      <div class="dropdown-menu bg-dark"  aria-labelledby="navbarDropdownMenuLink">
        <%= link_to "Articles", articles_path, class: "nav-link" %>
        <% if logged_in? %>
          <%= link_to "New Article", new_article_path, class: "nav-link" %>
        <% end %>
      </div>
      </li>

         <%= link_to "Bloggers", users_path, class: "nav-link" %>
    </ul>
  </div>
  <% if logged_in? %>
    <%= "(Admin)" if current_user.admin? %>
    <%= link_to current_user.username, user_path(current_user), class: "nav-link" %>
  <% end %>

</nav>

